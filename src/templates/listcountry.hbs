<h1 class="title">{{name}}</h1>
<div class="items-holder">
    <ul class="list-items">
        <li class="item">Capital: {{capital}}</li>
        <li class="item">Population: {{population}}</li>
        <li class="item">languages: <ul class="lang">
       {{#each languages}}
      <li class="lang__item">{{name}}</li>
      {{/each}}
    </ul></li>
    </ul>
    <div class="img-wrap"><img src="{{flag}}" alt="{{name}}"></div>
</div>